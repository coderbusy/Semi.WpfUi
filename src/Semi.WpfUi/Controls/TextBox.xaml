<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Semi.WpfUi.Controls">
    <ControlTemplate x:Key="TextBoxInnerContentTemplate" TargetType="ContentControl">
        <ContentPresenter
            VerticalAlignment="Center"
            Content="{TemplateBinding Content}"
            ContentStringFormat="{TemplateBinding ContentStringFormat}"
            ContentTemplate="{TemplateBinding ContentTemplate}"
            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
            RecognizesAccessKey="True"
            TextElement.Foreground="{TemplateBinding Foreground}" />
    </ControlTemplate>

    <Style x:Key="TextBoxInnerContent" TargetType="ContentControl">
        <Setter Property="Foreground" Value="{DynamicResource SemiColorText2}" />
        <Setter Property="Template" Value="{StaticResource TextBoxInnerContentTemplate}" />
        <Setter Property="Visibility" Value="Collapsed" />
        <Style.Triggers>
            <Trigger Property="HasContent" Value="True">
                <Setter Property="Visibility" Value="Visible" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="{x:Type local:TextBox}" TargetType="local:TextBox">
        <Setter Property="Height" Value="{DynamicResource SemiHeightControlDefault}" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="FontSize" Value="{DynamicResource SemiFontSizeRegular}" />
        <Setter Property="FontFamily" Value="{DynamicResource SemiFontFamilyRegular}" />
        <Setter Property="Foreground" Value="{DynamicResource SemiColorText0}" />
        <Setter Property="PlaceholderForeground" Value="{DynamicResource SemiColorText2}" />
        <Setter Property="Background" Value="{DynamicResource SemiColorFill0}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SemiColorBorder}" />
        <Setter Property="BorderThickness" Value="{DynamicResource SemiBorderThicknessControl}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource TextBlockSelectionBackground}" />
        <Setter Property="SelectionTextBrush" Value="{DynamicResource TextBlockSelectionForeground}" />
        <Setter Property="SelectionOpacity" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:TextBox">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource SemiBorderRadiusSmall}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentControl x:Name="LeftContent" Content="{TemplateBinding InnerLeftContent}" />
                            <Grid Grid.Column="1">
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                    VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}" />
                                <TextBlock
                                    x:Name="Placeholder"
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Foreground="{TemplateBinding PlaceholderForeground}"
                                    IsHitTestVisible="False"
                                    Text="{TemplateBinding PlaceholderText}"
                                    TextTrimming="CharacterEllipsis"
                                    Visibility="Collapsed" />
                            </Grid>
                            <ContentControl
                                x:Name="RightContent"
                                Grid.Column="2"
                                Content="{TemplateBinding InnerRightContent}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SemiColorFill1}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SemiColorBackground0}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource SemiColorPrimary}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource SemiColorDisabledText}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SemiColorDisabledBackground}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="Transparent" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
